# TODO: replace with a real per-user/session token once auth is wired up.
# Note: Vite inlines VITE_-prefixed vars into the client bundle at build
# time, so this is not a secret in the deployed app - just kept out of git
# history and made configurable per-environment.
VITE_UTD_STATIC_BEARER_TOKEN=
